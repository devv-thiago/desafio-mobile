import 'package:flutter/material.dart';

class UpcomingCatalog extends StatelessWidget {
  const UpcomingCatalog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}